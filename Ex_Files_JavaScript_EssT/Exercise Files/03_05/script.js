var a = 5;
var b = 5; // try changing 5 to 4, also try change 5 to a string  
var theNumbersMatch

if ( a == b ) { // try using a single = sign, also try add an extra = sign (strict equality) if you have change 5 to a string and tested it
    theNumbersMatch = true;
} else {
    theNumbersMatch = false;
}

console.log(theNumbersMatch);
